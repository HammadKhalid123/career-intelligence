import { Link } from "react-router-dom";
import { useResume } from "../hooks/useResume";
export default function Dashboard() { const { resume } = useResume(); const name = resume?.parsedData?.name?.split(" ")[0] || "there"; const skills = resume?.parsedData?.skills || []; return <div className="page dashboard"><section className="hero-card"><div><span className="pill">✦ YOUR AI CAREER PARTNER</span><h2>Hi, {name}.<br />Let’s move your career forward.</h2><p>Turn your resume into a focused job-search and learning strategy.</p><Link className="button" to={resume ? "/match" : "/resume"}>{resume ? "Analyze a job" : "Upload my resume"} →</Link></div><div className="hero-orbit"><div className="orbit-core">✦</div><i>Resume</i><i>Skills</i><i>Goals</i></div></section><section className="section-heading"><div><h2>Your career toolkit</h2><p>{resume ? "Your resume is ready. Choose the next step." : "Upload your resume to activate all tools."}</p></div></section><div className="tool-grid"><Tool to="/resume" icon="↥" title="Resume intelligence" text="Extract skills, experience, and education." /><Tool to="/match" icon="◎" title="Job match" text="See ATS fit and identify skill gaps." /><Tool to="/roadmap" icon="⌁" title="Career roadmap" text="Build a personalized learning plan." /><Tool to="/chat" icon="◌" title="Ask Copilot" text="Ask questions grounded in your resume." /></div>{resume && <section className="quick-card"><div><span className="eyebrow">PROFILE SNAPSHOT</span><h2>{resume.filename}</h2><p>{skills.length ? `${skills.length} skills identified` : "Your profile is ready for analysis"}</p></div><div className="badges">{skills.slice(0, 6).map(x => <span key={x} className="badge">{x}</span>)}</div><Link to="/resume" className="text-link">View profile →</Link></section>}</div>; }
function Tool({ to, icon, title, text }) { return <Link to={to} className="tool-card"><i>{icon}</i><h3>{title}</h3><p>{text}</p><span>Open →</span></Link>; }
