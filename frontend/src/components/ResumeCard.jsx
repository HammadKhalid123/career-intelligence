export default function ResumeCard({ data }) { if (!data) return null; return <section className="resume-card"><div className="resume-head"><div><h2>{data.name || "Your resume"}</h2><p>{[data.email, data.phone].filter(Boolean).join(" · ") || "Parsed resume"}</p></div><span className="verified">✓ Parsed</span></div><div className="resume-body"><Section title="Skills"><div className="badges">{data.skills?.map(x => <span key={x} className="badge">{x}</span>)}</div></Section><Section title="Experience">{data.experience?.map((x, i) => <div className="timeline" key={i}><b>{x.title || "Role"}{x.company && ` · ${x.company}`}</b><small>{x.duration}</small><p>{x.description}</p></div>)}</Section><Section title="Education">{data.education?.map((x, i) => <p className="education" key={i}><b>{x.degree}</b> · {x.institution} <small>{x.year}</small></p>)}</Section></div></section>; }
function Section({ title, children }) { return <div className="resume-section"><h3>{title}</h3>{children}</div>; }
